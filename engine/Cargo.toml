[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
gl = "0.14.0"
windowed = { path = "../vendor/windowed" }
probably_fine_log = { path = "../vendor/probably_fine_log" }
numix = { path = "../vendor/numix" }
rmmesh = { path = "../vendor/rmmesh" }
